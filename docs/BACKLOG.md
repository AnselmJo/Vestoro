# Backlog (ideas out of current scope)

- Code-splitting for ECharts (bundle is ~790 kB; fine locally, split for Pages)
- Saved generic import profiles matched by header signature
- vite-plugin-pwa: installable app, offline shell
- Transaction detail drawer with raw CSV data
- Rule editor (edit priority/field/op inline)
- Category colors + custom palette
